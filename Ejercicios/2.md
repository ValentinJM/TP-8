```mermaid
    graph TD;
    Main-->Número Real X
    Número Real X-->Input X
    Input X-->if x==0
    if x==0-->Imprimir "X no pertenece a ningún intervalo"
    Note right of Imprimir "X no pertence a ningún intervalo": si
    if x==0-->if -1<=x<0
    Note right of if -1<=x<0: no
    if -1<=x<0-->Imprimir "X pertence al intervalo [-1;0)"
    Note right of Imprimir "X pertence al intervalo [-1;0)": si
    Imprimir "X pertence al intervalo [-1;0)"-->end
    if -1<=x<0-->if 0<x<=1
    Note right of if 0<x<=1: no
    if 0<x<=1-->Imprimir "X pertence al intervalo (0;1]"
    Note right of Imprimir "X pertence al intervalo (0;1]": si
    Imprimir "X pertence al intervalo (0;1]"-->end
    if 0<x<=1-->Imprimir "X no pertenece a ningún intervalo"
    Note right of Imprimir "X no pertenece a ningún intervalo": no
    Imprimir "X pertence al intervalo [-1;0)"-->end
```
